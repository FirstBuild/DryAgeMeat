{
  "event": "dryage-tx-data",
  "url": "[SEE PROJECT TASK PAGE]",
  "requestType": "POST",
  "headers": {
    "x-api-key": "[SEE PROJECT TASK PAGE]"
  },
  "json": {
    "pub_time": "{{PARTICLE_PUBLISHED_AT}}",
    "hmdty": "{{hmdty_data}}",
    "mt_tmp": "{{mt_tmp_data}}",
    "mt_wt": "{{mt_wt_data}}",
    "amb_tmp": "{{amb_tmp_data}}"
  },
  "mydevices": false,
  "noDefaults": true,
  "rejectUnauthorized": true
}
